func main()
  var a,b,c: int
  var s : char
  var q : bool
  var A: array[10] of int
  var B: array[10] of int
  var C: array[5] of bool

  A[c] = B[q] - a(x);
  A = [ true ? b+1 : 0 for q in B ];
  if B[q] == C[b*a] then
     B = [ c>=0 ? a*c : b*(-c+1) for b in B ];
  endif
  C[a] = q or A[3.2+b]=='x';
  C = [ a>c ? a%2==0 : a*b>5 for s in A ];
  B = [ q or b!=3 ? b*2 : b/5 for q in C];
  b[5] = A[B[2]] + B[A];
endfunc
