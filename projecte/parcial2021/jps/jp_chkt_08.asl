
func main()
  var i : int
  var x,y: float
  var b: bool
  var t : {int, bool, float}
  var q : {float, int}

  t{0} = 3*(i+b);
  x{1} = q{0} > 5.2;
  t{1} = b{1} > 5.2;
  if x+i then
     q{0} = q{1}*x-1;
     y = not t{1};
  endif
  x = 2*t{2} + i{0};
  y{5} = 3.2;
endfunc
